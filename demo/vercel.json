{
	"buildCommand": "(cd .. && pnpm --filter why-render build) && pnpm run build",
	"outputDirectory": ".next",
	"installCommand": "pnpm install --frozen-lockfile",
	"ignoreCommand": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]",
	"framework": "nextjs"
}
