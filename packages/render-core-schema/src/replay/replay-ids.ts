export type ReplaySessionId = string;

// Composed as `${sessionId}:${frameIndex}` — uniquely identifies a frame across all sessions.
export type ReplayFrameId = string;

export type ReplayBookmarkId = string;
